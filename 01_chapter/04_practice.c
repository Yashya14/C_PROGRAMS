#include<stdio.h>
int main(){
    int principal = 100, rate =2 , year = 1;
    int simpleinterst = (principal*rate*year)/100;
    printf("The value of simple interest is %d\n",simpleinterst);

return 0;
}